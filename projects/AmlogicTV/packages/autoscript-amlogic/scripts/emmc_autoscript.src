if test ${bls} = android; then setenv bls "libreelec"; saveenv; run storeboot; fi;
setenv env_addr "0x10400000"
setenv bootargs ddr_size=${ddr_size} ${bootargs}
if fatload mmc 1 ${loadaddr} kernel.img; then if fatload mmc 1 ${env_addr} uEnv.ini; then env import -t ${env_addr} ${filesize}; setenv bootargs ${boot_env} ${bootargs}; fi; if fatload mmc 1 ${dtb_mem_addr} ${dtb_name}; then bootm ${loadaddr}; else store dtb read ${dtb_mem_addr}; bootm ${loadaddr}; fi; fi;
