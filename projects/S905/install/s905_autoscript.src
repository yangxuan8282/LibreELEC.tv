setenv bootargs_sd " bootfromsd "
setenv usbdtb "if fatload usb 0 ${dtb_mem_addr} dtb.img; then echo usb dtb.img loaded; else store dtb read $dtb_mem_addr; fi"
setenv sddtb "if fatload mmc 0 ${dtb_mem_addr} dtb.img; then echo sd dtb.img loaded; else store dtb read $dtb_mem_addr; fi"
if fatload usb 0 ${loadaddr} kernel.img; then run usbdtb; setenv bootargs  ${bootargs_sd} ${bootargs}; wipeisb; bootm; fi
if fatload mmc 0 ${loadaddr} kernel.img; then run sddtb; setenv bootargs ${bootargs_sd} ${bootargs}; wipeisb; bootm; fi
